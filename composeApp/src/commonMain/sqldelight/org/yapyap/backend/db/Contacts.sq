CREATE TABLE contacts (
    onion_address TEXT PRIMARY KEY,
    status TEXT NOT NULL
);

insertContact:
INSERT INTO contacts (onion_address, status)
VALUES (?, ?);

getAllContacts:
SELECT * FROM contacts;